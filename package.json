{
  "name": "lambda-test",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "exit 0",
    "deploy:dev": "sls deploy --stage dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless": "^1.10.1"
  }
}
